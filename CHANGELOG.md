# 更新日志

记录用户可见的功能、修复与升级注意事项，最新记录放在最前。后续提交在「未发布」中累积变更，发布时归档为日期或版本记录。仅修改源码不代表已发布安装包。

## 未发布

### 新增

- 会议详情新增本机「录音回放」：播放/暂停、进度拖动、前后 10 秒及 0.75–2 倍速。音频按需分段读取，不生成完整 WAV 副本或上传。
- 有有效时间信息的 Qwen 转写片段支持点击回听和当前片段标记；无时间信息的旧记录、本地转写及未对齐的飞书妙记保留整段回放。

- 会议详情新增「录音处理与恢复」：独立选择本地 Whisper 或 Qwen 云端转写，显示处理阶段、已保存分段和错误原因，支持取消后继续。
- 持久化录音路径、格式、文件字节数和处理状态；没有字幕但有录音的中断会议也能找回，重启只恢复记录，不自动上传。
- Qwen 已提交任务保存任务 ID，恢复时先查询；完成分段保存至本地。云端提交结果未知或任务失效时，须明确选择可能重复计费的重新提交，或改用本地处理。
- 本地 Whisper 也按 20 分钟分段保存进度；录音内容指纹变化时不复用旧结果。重新识别全部可忽略已有进度。

### 改进与修复

- 修复设计评审的三个优先问题：压缩录音回放，默认收起已完成会议的恢复工具与逐句片段，决策和要点放在长摘要之前；失败或待恢复录音仍展开恢复入口。
- 补齐导航、会议链接、筛选、开关、待办和图标按钮的语义与键盘焦点；弹窗支持 Tab 循环、Esc 关闭及关闭后恢复焦点。
- 转写设置改为「仅实时字幕 / 本地精转 / 云端精转」三选一，一次保存完整模式；兼容旧配置，缺少 Qwen 密钥时明确显示本地回退。密钥存在与服务连接验证分别表述。

- 回放期间暂停自动起录，并阻止手动同时录制；离开会议释放播放访问。录音缺失或格式不支持时明确提示，已有文字仍保留。

- 会议和设置改为原子写入并保留备份；损坏时尝试恢复备份，均不可读时停止写入并提示，不再静默重建空库。
- 录音在等待转写和网络前先关闭并落盘；新录音用会议 ID 关联，兼容旧时间戳文件，避免重复恢复。新增删除记录标记，防止今后删除的会议再次被恢复。
- 新转写成功前保留原稿，替换前另存旧版本；已有纪要和待办继续保留，提示纪要是否需要按新转写重新生成。
- 「重新生成纪要」只重试纪要；转写失败与纪要失败分别保存。取消会中止本地处理/当前云请求，但已提交云任务可能继续运行并计费。
- 增加单实例保护和处理互斥，避免录制与恢复任务同时修改同一份记录。

### 验证与限制

- 本轮 37 项离线测试、类型检查和生产构建通过；新增模式兼容、缺少密钥回退、原子保存及非法模式拒绝测试。

- 新增备份损坏、空字幕恢复、旧数据迁移、长录音续转、取消、云端未知提交与跨进程崩溃恢复测试；云请求使用模拟服务，不上传私人录音。
- 真实本地 Whisper 已使用 29 秒合成语音验证。识别仍可能出现同音字；静音/空结果会保留原稿并提示，不能据此保证录音内容准确。
- 33 项离线测试通过；桌面测试覆盖播放、1.5 倍速、20 秒与 2 小时片段定位、键盘进度调整和播放期间录制互斥。使用独立合成资料，不上传私人录音。
- 本次不包含人工纪要编辑；转写前旧版本保存在本机 processing 目录，尚无版本浏览界面。

## 2026-09-25

### 新增

- 接入 OpenAI Responses API：Luna 用于普通纪要、问答与文本校对，Sol 用于深度分析与会前关注点；会议详情展示实际使用的模型。
- 接入 Qwen 云端精转，支持设置开关、长录音每 20 分钟分段、说话人分段结果与失败原因展示。
- 设置页新增麦克风和录屏权限检查、系统设置入口、重新检查与系统音频实际采集测试。
- 录制页新增系统声音和麦克风独立音量、数据接收状态、麦克风混入状态，以及连续 30 秒无明显声音提醒。
- 实时字幕原地更新，结束片段由本地 Whisper 校对，可展开查看识别原稿；校对积压、结果为空或明显缩短时保留原稿。

### 修复

- 系统音频不可用时正确尝试麦克风回退；修复立体声音频缓冲处理，改善采集进程退出、数据中断检测及重复启动保护。
- Qwen 某段返回 `ASR_RESPONSE_HAVE_NO_WORDS` 或空结果时，尝试本地 Whisper 恢复该段并继续后续分段；云端与本地均无结果时保留错误提示，避免静默丢段。
- 飞书日历使用正确日期参数，扩展 CLI 查找路径，区分未安装、未配置与授权失败。
- 修复流式识别跨数据块的文字解码和分段边界处理，打包时使用随应用更新的识别脚本。
- 重新生成纪要时尽量保留已有待办完成状态，改善长文本处理和模型异常提示。

### 配置与升级

- 密钥不再打包进应用；已安装应用的配置放在 `~/Library/Application Support/AfterMeet/.env`，变量示例见 `.env.example`。
- 打包使用稳定的本地签名证书，减少更新后的权限身份失配；首次切换签名身份仍可能需要移除旧权限条目、重新授权并完全退出重启。
- 开启 Qwen 云端精转会上传音频；AI 纪要会向模型服务发送转写文本。本地实时字幕校对不额外上传音频。
- 分段说话人编号仅在各段内有效；实时字幕仍可能出现同音字、重复或幻觉，重要内容需核对录音。

### 验证

- 新增 `npm run test:cloud` 离线回归测试，覆盖模型请求、Qwen 分段恢复、字幕校对回退与音量状态过期，不调用付费接口。
- 已进行 macOS 本机系统采集、实时字幕、Qwen 精转和 Luna 纪要的录制链路验证；该验证不代表所有设备与音频条件下的识别准确率。
